# Host list template
# Copy to YourEnvironment.hosts (name must match an entry in suffixes.conf)
# One hostname per line, comments start with #

# Short hostnames get the suffix from suffixes.conf appended:
bastion1
appserver1
appserver2
database1

# FQDNs (containing '.') are used as-is:
# special.other.domain.com

# user@host format is also used as-is:
# admin@jumpbox.example.com
